@model IEnumerable<finalDBMSPRo.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Or_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Or_Item)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.LastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Or_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Or_Item)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.OrId }) |
            @Html.ActionLink("Details", "Details", new { id = item.OrId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.OrId })
           
        </td>
    </tr>
}

</table>

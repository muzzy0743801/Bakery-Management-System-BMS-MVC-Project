@model IEnumerable<finalDBMSPRo.Models.ProductIngredient>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Proc_Ing_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proc_Ing_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Pro_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Proc_Ing_id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Proc_Ing_Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Product.Pro_Name)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Proc_Ing_id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Proc_Ing_id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Proc_Ing_id })
        

    </td>
</tr>
}

</table>
